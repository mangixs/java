spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/database?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&zeroDateTimeBehavior=convertToNull
    username: root
    password: root
  thymeleaf:
    cache: false
    mode: HTML5
  redis:
    # Redis数据库索引（默认为0）
    database: 0
    # Redis服务器地址
    host: 127.0.0.1
    # Redis服务器连接端口
    port: 6379
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
        max-wait:
    password: ''
  mongo:
    connectionsPerHost: 5000
    dbname: admin
    mongoDatabaseAddress: localhost:20017
    password: root
    username: root
logging:
  level:
    com:
      example:
        demo:
          mapper: debug
mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8008

file:
  upload-dir: ./src/main/resources/static/image



