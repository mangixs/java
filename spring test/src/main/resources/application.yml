spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: yibai123456
    url: jdbc:mysql://192.168.71.216:3306/yb_datacenter?useUnicode=true&characterEncoding=UTF-8
  thymeleaf:
    cache: false
    mode: HTML5
  redis:
    # Redis数据库索引（默认为0）
    database: 0
    # Redis服务器地址
    host: 127.0.0.1
    # Redis服务器连接端口
    port: 6379
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
        max-wait:
    password: ''
  mongo:
    connectionsPerHost: 50
    dbname: hwc
    mongoDatabaseAddress: 192.168.71.239:27017
    password: hwc@2018
    username: hwcuser

mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8008
winit:
 account:

 list:
   - default
   - api
   - kevin


